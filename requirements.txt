# ─────────────────────────────────────────────────────────────────
# SNODE - AI-Powered Pentesting Agent
# Python Dependencies
# ─────────────────────────────────────────────────────────────────

# Core Dependencies
requests>=2.31.0
sqlalchemy>=2.0.0
PyYAML>=6.0

# LangChain + LLM
langchain>=1.0.0
langchain-core>=1.0.0
langchain-ollama>=1.0.0

# LangGraph (for workflows)
langgraph>=1.0.0
langgraph-checkpoint>=3.0.0

# ChromaDB (for CVE RAG)
chromadb>=1.0.0

# HTTP Client
httpx>=0.28.0

# Phoenix Tracing & Observability (Optional)
arize-phoenix>=4.0.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# ─────────────────────────────────────────────────────────────────
# External Security Tools (install separately)
# ─────────────────────────────────────────────────────────────────
# Install script: ./install_tools.sh
#
# Go Tools (install via `go install`):
#   - httpx, nuclei, subfinder, katana, dalfox, gau, arjun, naabu
#
# System Tools:
#   - nmap, nikto, gobuster, ffuf, rustscan, sqlmap
#
# See install_tools.sh for full installation commands
