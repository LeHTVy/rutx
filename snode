#!/bin/bash
# SNODE Launcher - Uses new modular /app architecture
# Get directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
source venv/bin/activate
python -W ignore::RuntimeWarning -c "from app.cli.main import main; main()" "$@"
