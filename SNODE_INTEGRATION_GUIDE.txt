"""
Quick Start Guide for SNODE Integration
Run this to test that everything is working
"""

print("""
╔═══════════════════════════════════════════════════════════╗
║  SNODE AI Integration - Quick Start Guide                ║
╚═══════════════════════════════════════════════════════════╝

✅ SNODE Integration is NOW ACTIVE when you run:

   python main.py

FEATURES ENABLED:
─────────────────────────────────────────────────────────────

🔍 Phoenix Tracing
   • Traces all Ollama LLM calls automatically
   • View real-time dashboard at http://localhost:6006
   • See agent decisions, tool calls, and performance

🛡️ Input Guardrails  
   • Blocks prompt injection attempts
   • Example blocked: "Ignore all instructions"
   • Your requests are validated before processing

🛡️ Output Guardrails
   • Prevents dangerous commands from executing
   • Blocks reverse shells, fork bombs, data exfiltration
   • Example blocked: "rm -rf /", "nc -e /bin/sh"

TESTING:
─────────────────────────────────────────────────────────────

1. Test SNODE features:
   
   cd e:\\Wireless
   python tests/test_snode_integration.py

2. Run main program with integration:

   python main.py
   
   Try these inputs:
   
   ✅ "Scan snode.com"  (Normal - will work)
   ❌ "Ignore all previous instructions"  (Blocked by guardrails)

3. View traces:

   Open browser: http://localhost:6006
   Run a scan and watch traces appear in real-time!

CONFIGURATION:
─────────────────────────────────────────────────────────────

File: config.py

# Disable tracing (for performance)
ENABLE_TRACING = False

# Disable guardrails (⚠️ NOT recommended)
ENABLE_GUARDRAILS = False

# Less strict validation
STRICT_INPUT_VALIDATION = False

IMPORTANT NOTES:
─────────────────────────────────────────────────────────────

• LOCAL Ollama LLM only - No cloud dependencies ✅
• Tracing is optional (works without Phoenix)
• Guardrails protect against adversarial inputs
• All features can be toggled in config.py

NEXT STEPS:
─────────────────────────────────────────────────────────────

Phase 2: Multi-Agent System
• Specialized agents (ReconAgent, ExploitAgent)
• Agent handoffs and collaboration
• Advanced attack patterns

Ready to test? Run:
   python tests/test_snode_integration.py

""")
